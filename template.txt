template <class TYPE>
TYPE smallest(TYPE arr[], int size)
{
          TYPE x = arr[0];
	for (int i =0; i<n; i++)
	{
	      if(x>arr[i])
                       x = arr[i];
	}
	return x;
}

//here TYPE is just a name, it'll be replaced by type of variable used.

	