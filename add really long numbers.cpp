#include <iostream>
#include <conio,h>
#include <mistack.h>
using namespace std;

int main()
{
	string num1;
	string num2;
	getline(cin,num1);
	getline(cin,num2);
	stack<char> s1(num1.length());
	stack<char> s1(num1.length());
	for(int i=0; i<)
	
}

